/*Напишите условие if для проверки, что переменная age находится в диапазоне между 14 и 90 включительно. */
let age = prompt('Введите число');
if (age >= 14 && age <= 90) {
    alert('Находится в этом диапазоне');
}
else {
    alert('Не находится в этом диапазоне')
}